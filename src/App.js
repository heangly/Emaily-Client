import { SERVER_GOOGLE_OAUTH_URI } from './constant/serverURL'

const App = () => {
  return (
    <div>
      <h1>App</h1>
      <a href={SERVER_GOOGLE_OAUTH_URI}>Google OAuth</a>
    </div>
  )
}

export default App
